% Evaluation-pipeline block diagram for "Small Models, Same Rules".
% Drop into the paper as a figure. Requires, in the preamble:
%   \usepackage{tikz}
%   \usetikzlibrary{positioning,arrows.meta,calc}
%
% \begin{figure*}[t]\centering \input{figures/pipeline.tex}
%   \caption{Evaluation pipeline. ...}\label{fig:pipeline}\end{figure*}

\begin{tikzpicture}[
    font=\small,
    box/.style={draw, rounded corners, align=center, inner sep=4pt,
                minimum height=9mm, text width=24mm},
    >={Stealth[]},
    node distance=7mm and 12mm]

  % inputs
  \node[box] (harm)   {HarmBench-200\\\scriptsize attack prompts};
  \node[box, below=of harm] (benign) {XSTest-250 /\\OR-Bench-Hard\\\scriptsize benign prompts};

  % grid + generation
  \node[box, right=of $(harm)!0.5!(benign)$, text width=27mm] (grid)
       {4 models $\times$ 2 conditions\\[2pt]\scriptsize Llama-3.2-3B, Phi-3-mini,\\\scriptsize Qwen2.5-3B, Gemma-2-2B\\\scriptsize empty / defensive prompt};
  \node[box, right=of grid, text width=22mm] (gen)
       {Generate\\\scriptsize greedy, $T{=}0$,\\\scriptsize $\le$256 tokens};

  % judges
  \node[box, right=of gen, yshift=11mm, text width=22mm] (j1) {Keyword baseline};
  \node[box, right=of gen, text width=22mm]              (j2) {Llama-Guard-3-1B};
  \node[box, right=of gen, yshift=-11mm, text width=22mm](j3) {HarmBench classifier};

  % verdicts + metrics
  \node[box, right=48mm of gen, text width=22mm] (verd) {Per-prompt,\\per-judge verdicts};
  \node[box, right=of verd, yshift=9mm, text width=24mm]  (asr) {ASR\\\scriptsize attack-side; 3 judges};
  \node[box, right=of verd, yshift=-9mm, text width=24mm] (frr) {FRR\\\scriptsize benign-side; kw + L-Guard};
  \node[box, right=of $(asr)!0.5!(frr)$, text width=26mm] (out)
       {Defense cost $C(M)$, $A(M)$;\\\scriptsize ranking-flip across judges};

  % edges
  \draw[->] (harm)   -- (grid);
  \draw[->] (benign) -- (grid);
  \draw[->] (grid)   -- (gen);
  \draw[->] (gen) -- (j1); \draw[->] (gen) -- (j2); \draw[->] (gen) -- (j3);
  \draw[->] (j1) -- (verd); \draw[->] (j2) -- (verd); \draw[->] (j3) -- (verd);
  \draw[->] (verd) -- (asr); \draw[->] (verd) -- (frr);
  \draw[->] (asr) -- (out);  \draw[->] (frr) -- (out);
\end{tikzpicture}
