# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# Experiment run outputs (regenerated by reproduce.sh; commit final ones with `git add -f`)
results/

# Encrypted completions sink — never commit raw model completions
*.enc
*.fernet

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Caches
.cache/

# Rendered / build artifacts and local-only deliverables (kept off GitHub)
paper_review_draft.pdf
paper_arxiv.pdf
paper_arxiv.tex
*.docx
PAPER_INFO.txt
_review_*.html
combined-*/
combined-*.zip
